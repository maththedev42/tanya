# Tanya Gate-Escape Analysis — the quality gates shipped and the very next run walked through them

> Prompt for an agent working in THIS repo (`/Users/matheus/Desktop/Projetos/Appzinhos/tanya`). Sequel to `RELIABILITY_HARDENING_PROMPT.md`, whose gates landed as 0.17.1-beta.9 (`ab8284f..e2c6be9`, released 2026-07-13 19:35). Between 20:10 and 20:26 the same day — 35+ minutes AFTER release — Tanya ran a batch of tasks on CosmoKit and reproduced the exact failure modes the gates were built to stop. This is a forensic task: for each escape, determine WHY the gate did not fire, close the hole, and pin it with a regression test. Do not add new gate categories — make the existing ones actually bite.

## The evidence (CosmoKit repo: `/Users/matheus/Desktop/Projetos/Appzinhos/cosmohq-project/CosmoKit`, app repo = nested git at `CosmoKit/`)

All timestamps 2026-07-13, gates released 19:35:19 -0300.

**E1 — A run ended with its entire deliverable uncommitted and a dependency untracked; no gate failed it.**
The "FIX2-01 precursor" run edited 9 source files (mtimes 20:10–20:22) implementing a Getting Started feature repair, committed **nothing**, and left `CosmoKit/Features/GettingStarted/GettingStartedManager.swift` **untracked — for the second run in a row** — while already-committed code (`GettingStartedView.swift`, committed earlier as part of an unrelated commit) references it. Verified consequence: `git worktree add … HEAD && xcodebuild` → **BUILD FAILED** (3 compile failures). The commit-completeness gate (`bcc1cfc`) and clean-tree build check (`4270349`) both existed at run time. Hypotheses to test, in order: (a) the gates only evaluate on the commit path, so a run that never commits bypasses them entirely; (b) the run ended via the stall path and `48a23e3`'s stall-path wiring has a hole; (c) the live npm-linked binary was stale (built before 19:35 — check `dist/` mtimes vs run logs); (d) these gates are configured off for ad-hoc/interactive runs. Find the actual cause in Tanya's run logs/telemetry, state it in your report with evidence, and fix it so that: **a run cannot report anything but FAILURE while files it created are untracked or files it edited are uncommitted — whether or not it ever reached a commit step.**

**E2 — Localization parity gate (in `ab8284f`) missed ~20 missing keys, twice.**
The run's diff uses ~20 new `L10n.tr("…")` keys in `GettingStartedView.swift`; zero entries exist in any of the four `*.lproj/Localizable.strings`. The prompt explicitly demanded all four. Check: does the parity gate only compare keys BETWEEN existing `.strings`/`strings.xml` files (parity among locale files), rather than extracting new localization keys from the *source-code diff* and requiring their presence in every locale file? If so, that's the hole: a key that exists in NO locale file is "in parity". Extend the gate to source-diff extraction (`L10n.tr`, `L10n.format`, `NSLocalizedString`, `stringResource`) — this exact shape has now shipped twice.

**E3 — Spec-coverage gate (`c0550f5`) passed a run with ~40% of numbered items unfinished, including one item stubbed with an empty `if` + comment.**
The FIX-01 prompt had 10 numbered items. Items 5 (half — availability never parsed, model defaults `isAvailable = true` so the new filter is a no-op), 6 (`Task {}` wrappers that inherit MainActor — the "off-main" fix moves nothing off main), 7 (partial: one event's attribute reads a consume-once flag *after* another call site consumed it → always `"false"`; the xcode-step event is literally `if … { // comment }` with an empty body; a previously-tracked `onboarding_dismissed` event was **deleted** in a rewrite), 8 (localization, fully skipped), and 10 (the commit itself) were not done. Questions: does the spec-coverage gate parse numbered items from the incoming prompt, or only from Tanya's own plan (which may silently omit items)? Does it accept "done" claims without evidence pointers? Does a run that ends on stall skip the manifest check? Fix so the manifest is derived from the PROMPT's numbered/bulleted requirements, each item needs a machine-checkable evidence pointer (file:line or command output), and prompt-mandated verification commands (a "## Verify"/"Verification Contract" section) MUST be executed with output captured (`d6be460` claims this — find why it didn't run here).

**E4 — Reachability/liveness nudges (`d09862b`) flagged nothing on: an empty-body `if` stub standing in for a specced analytics event; a filter on a field that is never assigned (constructor default makes it constant-true); and the silent deletion of an existing analytics emit during a rewrite.**
Add three cheap detectors: (1) empty conditional body containing only a comment in newly added code → nudge; (2) newly added struct/model field with a constant default that no call site ever sets → nudge ("field is constant — filter/branch on it is dead"); (3) an `Observability.track`/analytics emit present in the pre-image of a rewritten function but absent in the post-image → nudge ("event deleted — intentional?").

**E5 — Batch/queue truncation is still invisible.**
Of the queue documented for the session (FIX-01, FIX-02, FIX-03, TANYA-02, TANYA-04, TANYA-07 — plus TANYA-05 and FIX-04 which did complete), only 3 tasks produced commits and FIX-02/FIX-03 left no trace at all. If those prompts were sent and dropped, nothing surfaced. Verify what the session actually received (run logs); if Tanya received and dropped them, extend the queue summary (report honesty, `48a23e3`) so every received prompt appears with executed / skipped / not-reached status. If they were never sent, say so in your report — that part is then a user-workflow issue, not a gate hole.

## Second evidence set — appcreator/cosmohq-v3 hosting run, SAME NIGHT, also under beta.9 (audited 2026-07-14)

An independent run (appcreator repo, `.tanya/runs/r-mrjqkw93-73d035b0*` lineage, commits 21:48–21:50) executed a 10-item prompt (`cosmohq-v3/.planning/hosting-providers/TANYA-11-Hosting-Round2-Fixes.md`) with explicit **commit-after-each-item** instructions. Two more escapes, cross-confirming E1/E3 from a different repo and language:

**E6 — A run that DID commit still broke the committed tree: partial commit with untracked dependencies.**
Commit `f202e5f` (appcreator) registers routes to `rd.PostDetectHosting` / `rd.PostDatabaseProvision` / `rd.PostDatabaseImport`, but the defining code was left behind: `api/pkg/reads/database_handler.go` UNTRACKED, the `PostDetectHosting` hunk uncommitted in `reads/handlers.go`. Verified: `git worktree add … HEAD && go build ./...` → `rd.PostDetectHosting undefined` +2. So the E1 hypothesis space narrows: this run took the COMMIT path (three commits!) and neither commit-completeness (`bcc1cfc`) nor the clean-tree build check (`4270349`) fired. Either the gates check "did the run commit *something*" instead of "is every file the run touched committed AND does HEAD compile", or the clean-tree check didn't run for this repo/language, or verdicts were nudge-tier and ignored. Additional required property: **a commit whose staged set references unstaged/untracked symbols must be blocked or auto-completed** — "a commit must carry its dependencies."

**E7 — Ordered-item truncation with repeat-offense items, again invisible.**
Of TANYA-11's 10 numbered items: 1–3 done+committed, 8 done but left uncommitted, 4/5/6/7/9/10 silently dropped. Items 6 and 7 (GitHub-prep env guard, goose parity in the CI workflow) have now been explicitly requested and silently dropped in **four separate prompts across three days**. The spec-coverage gate (`c0550f5`) raised nothing visible. Same fix direction as E3 (prompt-derived manifest + per-item evidence), plus: when the SAME normalized deliverable appears in consecutive prompts (retry semantics), the coverage report should mark it `repeat-offense` — these are the highest-signal items a run must not drop.

## Deliverables

1. **Forensic report first** (in your final message AND as `docs/gate-escape-2026-07-13.md`): for E1–E5, the root cause with evidence from Tanya's own run logs/state (find where runs log gate execution; if gates don't log their verdicts, that's itself a finding — make them log). Distinguish "gate never executed", "gate executed and passed wrongly", "gate nudged but verdict ignored", and "stale binary".
2. **Fixes** in `src/agent/` (gate modules from `ab8284f..e2c6be9`: `dodGate.ts`, `acceptanceCriteria.ts`, the quality/commit-completeness/spec-coverage/verify-gate modules) closing each confirmed hole. Priorities: E1 (run-end evaluation independent of commit path) > E3 (prompt-derived manifest + mandatory Verify execution) > E2 (source-diff key extraction) > E4/E5.
3. **Regression tests** reproducing each escape from the real shapes above (fixture: repo with untracked file referenced by tracked code + run that ends without committing; diff with `L10n.tr` keys absent from all locales; 10-item prompt with an empty-if stub and a skipped item; rewrite that drops a track call; queue of 6 with 3 unexecuted). Red before your fix, green after.
4. Repo conventions: existing test framework, `CHANGELOG.md`, version bump, `npm run build` + full suite green, path-limited commits, no push. **After building, verify the linked binary picked up the change (`tanya --version` or equivalent) — a stale npm-link binary is hypothesis (c) for E1 and must be ruled out or confirmed.**

## Constraints

- Keep the self-clearing philosophy for heuristics (E4 = nudge tier), but E1/E2/E3 are objective facts — hard-fail tier, default ON for ad-hoc runs (same decision as the commit gate, 6d1472a).
- A working app must never be false-failed: the clean-tree build check may be expensive — run it when the session touched a compiled-language repo, cache per commit SHA, and let repos opt out explicitly (not implicitly).

---

## ROUND-3 ADDENDUM (2026-07-14) — evidence hardened, one hypothesis eliminated

A third CosmoKit run (2026-07-13 ~21:54–21:57, the "FIX2-01" task) repeated the signature and added a worse one:

**E1 escalation — three consecutive runs, same shape.** `GettingStartedManager.swift` untracked for the THIRD run in a row; zero commits for the third time; localization skipped for the third time. The failure signature is perfectly consistent: Tanya completes the interesting mid-task engineering items and drops the closing items (strings files, staging, commit) — whatever ends the run (stall, turn budget, user interrupt) apparently ends it BEFORE the closing items, and no gate converts that into a FAILURE verdict. When you do the forensics, specifically determine HOW these three runs terminated (Tanya's run logs): if runs are being interrupted/stalled out, the gates must still evaluate on that path and the report must say "run ended early — N of M items done, nothing committed" instead of whatever it said.

**E8 (new; renumbered — E6/E7 are taken by the hosting evidence set above) — a run ended with the tree not compiling, and the mandated verify command was never executed.** The FIX2-01 prompt embedded a Verification Contract whose step 1 is a plain `xcodebuild` invocation that fails in seconds on this tree: the run changed `SimulatorDevice.init` to require a new `isAvailable:` parameter but never updated the two construction sites in `SimulatorAppsService.swift` (`:299`, `:456` — that file's mtime shows it was never touched in the run). Two hard compile errors. The verify-gate (`d6be460` — "required ## Verify commands must run with evidence") either did not execute, executed against nothing, or its failure did not block the verdict. This is the strongest single reproduction for E3/verify-gate forensics: a required, cheap, deterministic command whose execution would have failed the run instantly.

**Hypothesis (c) for E1 is ELIMINATED — do not spend time on it.** Verified 2026-07-14: `/opt/homebrew/bin/tanya` → npm-link symlink to this repo (`lib/node_modules/@matheuskrumenauer/tanya` → `Appzinhos/tanya`), installed version 0.17.1-beta.9, `dist/` built 19:34:58 (the beta.9 release build), and the gate identifiers (`commit-completeness`, `spec-coverage`) are present in the shipped `dist/` chunks. **The gates were in the live binary during every escaped run.** The remaining suspects are: (a) gates evaluate only on the commit path / normal-completion path, so no-commit and interrupted runs bypass them entirely; (b) gate verdicts are computed but don't block the final report; (d) gates off for this run mode. Find which, with log evidence.

Add to the regression suite: a run that terminates early/stalls with a required-Verify command unexecuted and a broken build must produce a FAILURE verdict naming the unexecuted command and the compile errors.
