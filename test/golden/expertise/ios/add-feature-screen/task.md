# Add Settings Feature Screen

## Workspace
iOS app: fixture/app/

## Intent
Implementar

## Goal
Add a new Settings feature screen at Features/Settings/SettingsView.swift. Use BrandedComponents and Theme tokens. Route the screen from RootTabView using the existing tab/navigation pattern.

## Constraints
- Follow the target app folder layout.
- Do not use inline hex colors or `Font.system(...)` in the feature view.
- Report every modified file.
